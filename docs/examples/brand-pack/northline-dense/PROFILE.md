# Northline-dense profile

Level claimed: L4
Light + dark: yes
Axes authored:
  - L1 colours + `--bw-color-surface-raised`
  - L2 font families
  - L3 radius + elevation (same material language as northline-material)
  - L4 `--bw-space-1` (`0.1875rem`)
  - L4 brand default density: `data-density="compact"` plus tighter
    `--bw-density-*` under that mode
Kit exceptions: none
Preview routes: shell, card, form, marketing band with
  `data-bw-brand="northline-dense"` and `data-density="compact"`;
  prefer `{% bw_token_specimen %}` plus live kit surfaces
Contrast: fg-on-accent light and dark (verify ≥ 4.5:1)
Deferred: spacious/comfortable brand tuning; page margins stay page-owned

Fictional L4 torture pack: material language plus tighter system rhythm.
Not a recommended product look. See [THEME.md](../../../THEME.md).
