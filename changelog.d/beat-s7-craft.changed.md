### Changed

- Beat S7 docs article craft: `examples/docs/article.html` now puts
  `{% bw_search %}` in the docs site header, an authored "On this page"
  TOC (`.bw-docs-toc`), dual warning and info callouts, and copyable
  code panels with richer fixtures. Shell CSS adds header search flex
  and the TOC composition pattern; code panel headers gain a surface
  fill. Does not claim a Syntax beat; independent F.2 re-score still
  required.
