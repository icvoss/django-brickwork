- **Clean-break 4.0.0 removals** (deprecated template block names,
  `--bw-font-size-3xs`, marketing dual-class chrome aliases). See the dated
  `[4.0.0]` section in `CHANGELOG.md` for the upgrade table and consumer
  actions. This fragment satisfies the PR changelog gate for the component
  template diff; do not re-assemble it into a later release.
