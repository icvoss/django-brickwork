- **Two accessibility defects the new W0.3 archetype harness caught in the shipped examples, fixed at source.** `examples/base.html` rendered `<title></title>` (WCAG 2.2 AA `document-title`) because its `page_title` block had no default, unlike every other archetype; it now defaults to `Northwind`. `.bw-auth__panel`/`.bw-centred__panel` left plain prose links (`examples/auth/reset.html`'s "Back to sign in" and equivalents) unstyled, so they fell back to the browser's UA blue against the dark theme's surface at 1.75:1 contrast; both panels now theme their links with `--bw-color-accent`/`--bw-color-accent-hover`, matching `.bw-marketing-footer__inner`'s existing `:where(a)` pattern (BR-BW-MKT-002).
