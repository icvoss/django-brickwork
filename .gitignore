# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
# Top-level Python build output only. Scoped with a leading slash so it does NOT
# catch src/brickwork/static/brickwork/dist/ (the shipped compiled artefacts).
/dist/
.coverage
.coverage.*
htmlcov/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.hypothesis/
*.sqlite3

# Virtualenvs
.venv/
venv/
env/

# Node / frontend
node_modules/
npm-debug.log*

# Editors / OS
.DS_Store
.idea/
.vscode/

# NOTE: src/brickwork/static/brickwork/dist/ IS committed and shipped (the
# compiled CSS/JS/token artefacts are the package's product). Do not ignore it.
# Style Dictionary token source lives in src/brickwork/tokens/source/ (also
# committed); only the top-level Python /dist/ build output is ignored above.

# Generated a11y fixtures + Playwright output
a11y/fixtures/
test-results/
playwright-report/
a11y-results.json

# Agent worktrees and local session state (umbrella convention: every repo
# gitignores .claude/, so worktree files never pollute the parent checkout).
.claude/

# Ad-hoc screenshots captured while driving the browser gates locally.
# Committed visual fixtures live under a11y/ and tests/, never at the root.
/*.png

# Playwright MCP session logs and page snapshots from local browser driving.
.playwright-mcp/
